import { useEffect } from 'react'
import LoginButton from '../Components/LoginButton'

function loginPage() {

    useEffect(() => {

    }, [])

    return (
        <div className="min-h-screen bg-[#e8e4fc] flex items-center justify-center px-5">
            <LoginButton />
        </div>
    )
}

export default loginPage

